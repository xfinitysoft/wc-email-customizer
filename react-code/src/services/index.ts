import './axios.config';
import { common } from '@xfinity/services/common';
import { article } from './article';
import {collectionApi} from './collection'

const services = {
  common,
  article,
  collectionApi,
};

export default services;
